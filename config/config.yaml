seed: 42
train:
    num_workers: 10
    run_report: '/project/outputs'
    n_epochs: 300
    patience: 50
    weight_decay: 0
    batch_size: 64
    lr: 0.000625
    valid_split: 0.2
    experiment_name: 'exp1'
model:
    drop_prob: 0.5
    final_conv_length: 'auto'
dataset:
    path: "/project/dataset/"

